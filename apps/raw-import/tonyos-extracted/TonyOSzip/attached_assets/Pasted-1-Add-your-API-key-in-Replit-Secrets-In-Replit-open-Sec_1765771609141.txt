1) Add your API key in Replit Secrets

In Replit, open Secrets (lock icon) and add:

Key: OPENAI_API_KEY

Value: your OpenAI key

Do not paste the key into any HTML/JS file. Keys must stay server-side. 
OpenAI Platform

2) Update package.json to include the OpenAI SDK

Replace your package.json with this (or add "openai" to dependencies):

{
  "name": "codebench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "openai": "^4.0.0"
  }
}


Then click Run so Replit installs deps.

3) Replace your server.js with this

This turns /api/ai into a real code assistant that returns clean JSON: { html, css, js, notes }

import express from "express";
import OpenAI from "openai";

const app = express();
app.use(express.json({ limit: "2mb" }));
app.use(express.static("public"));

app.get("/health", (_req, res) => res.json({ ok: true }));

const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

function safeJsonParse(text) {
  try {
    return { ok: true, value: JSON.parse(text) };
  } catch {
    return { ok: false, value: null };
  }
}

app.post("/api/ai", async (req, res) => {
  try {
    if (!process.env.OPENAI_API_KEY) {
      return res.status(500).json({
        error: "Missing OPENAI_API_KEY",
        fix: "Add OPENAI_API_KEY in Replit Secrets (lock icon)."
      });
    }

    const { prompt = "", html = "", css = "", js = "", mode = "improve" } = req.body || {};
    if (!prompt.trim()) return res.status(400).json({ error: "Missing prompt" });

    const instructions = `
You are Codebench AI. You must return ONLY valid JSON with this schema:
{
  "html": "string",
  "css": "string",
  "js": "string",
  "notes": "string"
}

Rules:
- No markdown, no backticks, no extra keys.
- Keep code runnable in a browser.
- Do not include any API keys.
- If mode is "debug", focus on fixing errors and making it run.
- If mode is "improve", improve clarity, UI, and reliability without breaking behavior.
- If mode is "build", create what the user asked for from scratch.
`;

    const input = `
Mode: ${mode}

User request:
${prompt}

Current HTML:
${html}

Current CSS:
${css}

Current JS:
${js}
`;

    const response = await client.responses.create({
      model: "gpt-5",
      reasoning: { effort: "low" },
      instructions,
      input
    });

    const text = response.output_text || "";
    const parsed = safeJsonParse(text);

    if (!parsed.ok) {
      return res.status(502).json({
        error: "Model did not return valid JSON",
        raw: text.slice(0, 4000)
      });
    }

    return res.json({ ok: true, ...parsed.value });
  } catch (e) {
    return res.status(500).json({
      error: "Server error",
      detail: String(e?.message || e)
    });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`Codebench running on port ${PORT}`));


This uses the Responses API (client.responses.create) exactly like the docs. 
OpenAI Platform
+2
OpenAI Platform
+2

4) Update public/app.js so AI gets your current code and can rewrite it

Find your callAI(prompt) function and replace it with this:

async function callAI(prompt, mode = "improve") {
  setStatus("AI working");
  aiOut.textContent = "Thinking...";

  try {
    const payload = {
      prompt,
      mode,
      html: editors.html.value || "",
      css: editors.css.value || "",
      js: editors.js.value || ""
    };

    const res = await fetch("/api/ai", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    });

    const data = await res.json();
    if (!res.ok) throw new Error(data?.error || "AI request failed");

    // Apply returned code into the editors
    if (typeof data.html === "string") editors.html.value = data.html;
    if (typeof data.css === "string") editors.css.value = data.css;
    if (typeof data.js === "string") editors.js.value = data.js;

    aiOut.textContent = data.notes || "Updated your code";
    run();
  } catch (e) {
    aiOut.textContent = "Error: " + (e?.message || e);
  } finally {
    setStatus("Idle");
  }
}


Then change your button handlers to pass a mode:

aiBtn.addEventListener("click", () => {
  const p = promptEl.value.trim() || "Improve this app without breaking it";
  callAI(p, "improve");
});

sendPrompt.addEventListener("click", () => {
  const p = promptEl.value.trim();
  if (!p) { aiOut.textContent = "Type a prompt first"; return; }
  callAI(p, "build");
});

5) Run it

Click Run in Replit.

Then type a prompt like:

Build a clean todo app with localStorage and a search bar

Debug why my button does nothing

Improve the UI to look like a modern SaaS dashboard

If it errors, paste the exact error message shown in the AI output box and I will tell you the one-line fix.